server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/LiveTalk
    username: postgres
    password: Arunda2021
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true